{
  "name": "applitools-hackathon",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "cy:run": "npx cypress run",
    "cy:run:v1": "npx cypress run --env version=v1",
    "cy:run:dev": "npx cypress run --env version=dev",
    "cy:run:final": "npx cypress run --env version=final",
    "cy:open": "npx cypress open",
    "cy:open:v1": "npx cypress open --env version=v1",
    "cy:open:dev": "npx cypress open --env version=dev",
    "cy:open:final": "npx cypress open --env version=final"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@applitools/eyes-cypress": "^3.15.5",
    "@types/node": "^14.14.7",
    "cypress": "^5.6.0",
    "typescript": "^4.0.5"
  }
}
